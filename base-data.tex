% -------------------------------
% Titel und Einstellungen für das Titelblatt
% -------------------------------

% \newcommand definiert einen neuen Befehl \titleText, der den Titel des Projekts enthält
\newcommand{\titleText}{Titel} 

% \newcommand definiert einen neuen Befehl \projectType, der den Typ des Projekts (z.B. Bachelorarbeit, Masterarbeit) enthält
\newcommand{\projectType}{Bachelorarbeit}

% \newcommand definiert einen neuen Befehl \degreeProgram, der den Studiengang enthält, in dem die Arbeit geschrieben wird
\newcommand{\degreeProgram}{Softwaretechnik und Medieninformatik}

% \newcommand definiert einen neuen Befehl \faculty, der die Fakultät oder den Fachbereich der Hochschule angibt
\newcommand{\faculty}{Informatik und Informationstechnik}

% \newcommand definiert einen neuen Befehl \authorName, der den Namen des Autors oder der Autorin der Arbeit enthält
\newcommand{\authorName}{Robert Rehberg}

% \newcommand definiert einen neuen Befehl \matriculationNumber, der die Matrikelnummer des Autors oder der Autorin enthält
\newcommand{\matriculationNumber}{768196}

% \newcommand definiert einen neuen Befehl \university, der den Namen der Hochschule oder Universität enthält
\newcommand{\university}{Hochschule Esslingen}

% \newcommand definiert einen neuen Befehl \firstReviewer, der den Namen des ersten Prüfers oder Betreuers enthält
\newcommand{\firstReviewer}{Prof. Dr. }

% \newcommand definiert einen neuen Befehl \secondReviewer, der den Namen des zweiten Prüfers oder Betreuers enthält
\newcommand{\secondReviewer}{Prof. Dr. }

% \newcommand definiert einen neuen Befehl \supervisor, der den Namen des betreuenden Professors oder der betreuenden Professorin enthält
\newcommand{\supervisor}{Prof. Dr. }

% \newcommand definiert einen neuen Befehl \universityLogoPath, der den Pfad zum Logo der Hochschule enthält
\newcommand{\universityLogoPath}{HS-Logo.png}

% \newcommand definiert einen neuen Befehl \companyLogoPath, der den Pfad zum Logo des Unternehmens enthält, in dem die Arbeit erstellt wurde (sofern zutreffend)
\newcommand{\companyLogoPath}{Bosch-Logo.pdf}

% \newcommand definiert einen neuen Befehl \uploadDate, der das Datum angibt, an dem die Arbeit abgegeben wird; \today wird automatisch das aktuelle Datum verwenden
\newcommand{\uploadDate}{\today}

% \newcommand definiert einen neuen Befehl \residence, der den Wohnort des Autors oder der Autorin enthält
\newcommand{\residence}{Ludwigsburg}

% -------------------------------
% Bedingte Anweisungen zur Anzeige bestimmter Inhalte
% -------------------------------

% \companytrue setzt den Schalter für die Verwendung eines Unternehmenslogos auf "wahr" (true),
% wodurch Inhalte, die das Unternehmenslogo betreffen, im Dokument angezeigt werden
\companytrue

% \reviewertrue setzt den Schalter für die Anzeige der Gutachter auf "wahr" (true),
% wodurch die Namen der Gutachter im Dokument angezeigt werden
\reviewertrue

% \supervisortrue setzt den Schalter für die Anzeige des Betreuers auf "wahr" (true),
% wodurch der Name des betreuenden Professors oder der betreuenden Professorin im Dokument angezeigt wird
\supervisortrue